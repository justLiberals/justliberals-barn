{% include "partials/icons.njk" %}

<div class="blog-header">
    <header class="container">
        <nav class="navbar navbar-light navbar-expand-xl">
            <div class="navbar-brand order-xl-last">
                <a class="navbar-brand order-xl-last" href="{{ site.url }}/">
                    <img class="site-logo" src="{{ site.url }}/{{ site.logo }}" alt="{{ site.title }}">
                </a>
            </div>
            <div class="navbar-header mr-auto collapse navbar-collapse"  id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    {% for item in site.navigation %}
                    <li class="nav-item">
                        {% if item.label %}
                        <a style="color: white;" class="nav-link {% if page.url == item.url %}active{% endif %}" {% if item.newtab == true %}target="_blank" {% endif %}href="{{ item.url }}">{{ item.label }}</a>
                        {% elif item.svg %}
                        <a style="color: white;" class="nav-link" {% if item.newtab == true %}target="_blank" {% endif %}href="{{ item.url }}"><svg class="icon" style="fill: white;" aria-hidden="true" focusable="false"><use xlink:href="#{{ item.svg }}"></use></svg></a>
                        {% endif %}
                    </li>
                    {% endfor %}
                </ul>
            </div>
        </nav>
    </header>
</div>

